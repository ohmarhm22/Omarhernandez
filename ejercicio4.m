clear all;
clc;
promt= ('Intoduce un valor: ')
a=input (promt);
promt= ('Intoduce un valor: ')
b=input (promt);
promt= ('Intoduce un valor: ')
c=input (promt);
if(a>b & a>c);
 sprintf('El numero mayor es:, %2f., ', a)

else
 if(b>a & b>c);
  sprintf('El numero mayor es:, %2f., ', b)
 
 else
  sprintf('El numero mayor es:, %2f., ',c)
 
 end
end
  
  